version: "3.9"
services:
  backend:
    build: .
    container_name: backend
    ports:
      - "8000:8000"
    env_file:
      - .env


  frontend:
    build: ../../../pokedesk-frontend
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend